package com.eaglesandcobras.common.util.graphics  {	import flash.display.Sprite;	import flash.display.Loader;	import flash.net.URLRequest;	import flash.events.Event;	import flash.display.BitmapData;		public class BGPattern extends Sprite {		private var loader:Loader;		private var bmdLoaded:Boolean;		private var bmd:BitmapData;		private var s:Sprite;				public function BGPattern(path:String, alp:Number = 1):void {			this.alpha = alp;						s = new Sprite();			addChild(s);						loader = new Loader()			loader.load(new URLRequest(path))			loader.contentLoaderInfo.addEventListener(Event.COMPLETE, patternReady)		}				private function patternReady(e:Event):void {			bmdLoaded = true;			bmd = new BitmapData(loader.content.width, loader.content.height, true, 0x00FFFFFF)			bmd.draw(loader);			position(_settings);		}				public function position():void {			//_settings = so;//			if (bmdLoaded) {//				// when stage changes size, redraw the background//					with(s.graphics){//						clear();//						beginBitmapFill(bmd);//						drawRect(0, 0, so.width, so.height);//						endFill();//					}//				}//			s.mouseEnabled = false;		}	}}